[pytest]
markers =
    e2e: Playwright end-to-end тестове (изискват стартирано приложение)
    snapshot: проверки върху ЗАПИСАНИТЕ прогони — падат, докато те са от стара
        версия.  Не блокират разработката; пускат се преди пакет за одитора:
        pytest -m snapshot
testpaths = tests
