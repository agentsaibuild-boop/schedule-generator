# Брийф за одитора — 31.08.2026

**Обхваща периода 18.08 – 31.08.2026** (82 комита след раздел 13 на предишния
брийф).  Раздел 14 е добавен след Вашето становище от същия ден и съдържа
какво е вложено по него. Предишните документи остават в сила там, където този не ги отменя:
`docs/BRIEF_ZA_ODITORA_2026-08-07.md` (раздели 1–13) и
`docs/PISMO_ZA_ODITORA_2026-08-10.md`.

Всяко число тук е измерено с команда, която можете да повторите. Където нямаме
доказателство, го пишем като нямаме доказателство.

---

## 1. Накратко

Периодът смени въпроса. Дотук питахме „минава ли графикът структурните
флагове". Отговорът вече е „да" и на двата обекта. Сега питаме „изкарва ли
системата график, който изпълнителят би подал на търг".

При подготовката на този брийф намерихме два дефекта, които никой флаг не
хващаше — празен критичен път и загубени клонове заради латински букви в
кирилски етикети. **И двата са поправени и премерени** (раздели 7 и 7а). Не ги
скриваме зад поправката: намерени са късно и щяха да излязат при Вас.

| | Илиянци | Тръстеник етап 2 |
|---|---|---|
| обявено в документите | 120 + 660 = **780 дни** | 45 + 210 = **255 дни** |
| нашият график | 120 + 660 = **780 дни** | 45 + 210 = **255 дни** |
| структурни флагове | 12/12 | 12/12 |
| проследимост до КСС | 100 % | 100 % |
| критичен път | 780 дни | 255 дни |
| човешкият график | 780 дни | — (реален търг) |

**Но:** съвпадението ден в ден НЕ е доказателство за качеството на мрежата.
Срокът е ВХОД, не резултат — виж раздел 3. Това е най-важното изречение в
документа и го пишем сами, преди да го намерите.

Три неща, които се промениха структурно:

1. **Моделът вече почти не участва.** Когато клоновете са известни, целият
   график се произвежда от кода — 0 повиквания, $0.00, 16 секунди (раздел 8).
2. **Продължителностите вече не идват от нормите.** Води обявеният срок.
   Колко от графика стъпва на какво основание — раздел 4, преброено наново по
   стълбицата на одитора в **раздел 14.2**: 96 % на доказателство, 33 задачи
   наистина без основание.
3. **Научихме се от 22 изпълнени графика на самия изпълнител**, вместо от един
   еталон (раздел 5).

---

## 2. Какво произвежда системата днес

Тръстеник етап 2, реален търг, произведен изцяло от системата:

```
вход:     107 отсечки, 15 846 м (11 662 водопровод + 4 184 канализация)
          9 реда КСС, въведени на ръка (14 числа, без нито един файл)
изход:    114 пакета, 1030 задачи, 255 дни
екипи:    10 водопроводни + 3 канализационни, раздадени динамично
```

```
фаза Проектиране      ден   1–45   =  45 дни при обявени 45
фаза Строителство     ден  46–255  = 210 дни при обявени 210
фаза Мобилизация      ден  46–49   =   4 дни
фаза Авторски надзор  ден  46–255  = 210 дни
фаза Приемане         ден 239–255  =  17 дни
```

Повторяемо с:

```
python tools/offline_dry_run.py --project <папка> --segments <клонове.json>
```

Комплектът за MS Project (`.xml` → `.mpp`) излиза от същия прогон.

---

## 3. Признанието: срокът е вход, не резултат

Правилото, което изпълнителят обяви на 24.08, е:

> „Когато има зададени срокове за проектиране и строителство, те трябва да се
> използват МАКСИМАЛНО. Ако проектирането е записано 100 дни, ние в графика го
> записваме 100 дни."

Търговски това е безспорно: обявеният срок е таван, който възложителят приема;
по-кратък поема риск без полза, по-дълъг прави офертата неотговаряща.

Методологически обаче то обръща посоката на сметката. Досега мрежата
произвеждаше срок; сега срокът произвежда темпо:

```
темпо = метри ÷ обявени дни ÷ брой екипи
```

Следствията, които трябва да знаете:

* **Съвпадението с човешкия график е тавтологично.** И ние, и човекът пишем
  780, защото и двамата четем 780 от документа. То доказва, че системата
  спазва обявеното — нищо повече. Оттегляме всяко предишно внушение, че
  съвпадението валидира нормите или мрежата.
* **Нормите станаха второстепенни.** Виж следващия раздел.
* **Критичният път се изпразни.** Виж раздел 7 — това е дефект, не свойство.

---

## 4. Откъде идват продължителностите днес

Измерено на 21.08 при изваждането на пълния опис (189 стойности в 11 раздела).
Всяко следващо правило надделява над предишното:

| ред | източник | какво покрива |
|---|---|---|
| 1 | норма по DN + материал + метод (`productivities.json`) | само тръбна задача с дължина, разпознат диаметър и материал |
| 2 | **`median_days` на веригата** (`tech_chains.json`) | **822 от 1123** продължителности |
| 3 | `scale_segment_overhead` | свива фиксираните стъпки при раздробен участък |
| 4 | **`calibrate_to_declared_pace`** | **надделява над всичко** |
| 5 | `DEFAULT_MIN_DAYS = 5` | под за параметрична задача |
| 6 | терен/условия | ИЗКЛЮЧЕНИ (25 неверифицирани коефициента) |

На днешния прогон за Тръстеник: **67 продължителности по норма, 821 без
доказателство** (`NOT_PARAMETRIC` 782, `NO_PRODUCTIVITY_RULE` 39).

> **Този разрез е подвеждащ и е заменен — виж раздел 14.2.** `NOT_PARAMETRIC`
> значи „няма норма", а се четеше като „няма доказателство". Медианата от 22-та
> изпълнени графика Е основание. По стълбицата на одитора същият прогон дава
> **96 % на доказателство и 33 задачи без основание**, а отделно от това 447
> задачи са местени заради срока.

Тоест верифицираният файл с производителности, който беше центърът на
предишните три брийфа, днес решава под 6 % от графика. **Поправка само на
нормите по диаметър няма да смени срока.** Кой източник да е водещ е въпрос
към Вас (раздел 11.2).

Отделно: безизкопното полагане се смята като открит изкоп, а норми има само за
3 диаметъра — **29 % от количествата в търга нямат норма**.

---

## 5. Корпусът: 22 изпълнени графика вместо един еталон

Изпълнителят даде цялата си папка с изпълнени процедури (23 обекта, 26 файла
`.mpp`). Извадени са през самия MS Project (`tools/read_mpp.py`) — 22 графика,
от 47 до 1402 задачи, от 175 до 808 дни.

Премерено, не предположено:

* **темпо на участък: медиана 8.4 м/ден** за целия цикъл (77 участъка с
  записана дължина); 25-и персентил 5.8, 75-и 9.8, максимум 14.4;
* **стъпки на участък: медиана 6** (диапазон 6–10);
* **работа на участък: медиана 8 дни** (6–43);
* **връзки: 93 % FS**, 7 % SS, 3 % FF, една SF; лагове има и са пъстри;
* дълбочина на WBS: 3–5 нива.

Медианите по дейност съвпадат с нашите `tech_chains` (канал 11 срещу 11.5 дни,
водопровод 12 срещу 12). Тоест **корпусът подкрепя ПРОДЪЛЖИТЕЛНОСТИТЕ на
основните ни технологични вериги**.

Нарочно не пишем „веригите ни са верни" — това би изисквало да сме сравнили и
самите операции, последователностите, типовете връзки и лаговете, семантиката
на еднократната работа и приложимостта на пакетите. Съвпадението на две медиани
не е такова доказателство. (Формулировката е по бележка на одитора, 31.08.)

**Еднократната работа.** Корпусът потвърди правилото и поправи един наш грешен
запис: при довеждащ/тласкател дезинфекцията е ×1 и присъединяването ×1, но
изпитването е ×10–14. Затова `once_per_route` остана на дезинфекция и
присъединяване и беше СВАЛЕНО от изпитването.

---

## 6. Чертежите се четат детерминистично, не с vision

**Оттегляме извода от предишния период, че за геометрия трябва DWG/DXF.**
Измерено с PyMuPDF върху тръжните ситуации:

```
Ситуация Вод.pdf    1 стр  15 175 знака текст  78 770 векторни обекта  0 растерни
Ситуация Канал.pdf  1 стр   9 712 знака текст  55 148 векторни обекта  2 растерни
```

Чертежите не са сканирани. Текстът е машинно четим с координати, линиите носят
цвят и дебелина (14 разделими пера), мащабът е изписан в самия чертеж. Vision
се проваляше, защото беше грешният инструмент, а не защото данните липсват.

Днес: първо детерминистичен четец (безплатен, повторяем, обяснява всяко
отпадане), vision само за чертеж, който той не разбира. Легендата решава кои
линии са на тази процедура — чертежът показва и съществуващите мрежи (Σ на
`L=` е 172 % от КСС, ако не се филтрира).

---

## 7. Дефект №1: критичният път беше празен — ПОПРАВЕН

Това е най-сериозната находка в периода и я даваме първи.

На чистия график за Тръстеник (255 дни, 12/12 флага):

```
листа: 895        критични: 1        нулев резерв: 1
най-чести резерви: 135, 143, 138, 153, 147, 183 дни
единствената критична задача: „Приемане на обекта (Акт 15/16)", ден 255
```

Тоест графикът, който излиза за MS Project, **няма критична верига**. Всяка
задача има 130–180 дни резерв, а крайната дата се държи от една точка.

Диагнозата е точна и проверима. В `src/ai_processor.py`:

```
1503:  cpm = builder.compute_critical_path(tasks)     ← смята резервите
1510:  tasks, fit_notes = fit_contract_span(tasks)    ← МЕНИ датите след това
1516:  tasks, sync_notes = sync_durations_to_span(tasks)  ← и продължителностите
```

CPM се смята ПРЕДИ налагането на договорния срок. След това датите се менят и
записаните `total_float` / `is_critical` описват график, който вече не
съществува. Личи си по невъзможно число: задача, която свършва на ден 255 при
проект, свършващ на ден 255, носи резерв 103 дни.

**Какво значеше за възложителя.** Критичният път в подадения `.mpp` не можеше
да се ползва за анализ на закъснения. Самите дати и срокът бяха верни;
описанието кое ги държи — не.

### Поправката и числата

Сметката на критичния път вече е НАКРАЯ, върху датите, които остават. Мерено
на същите два обекта, с еднакви входни данни преди и след:

| | преди | след |
|---|---|---|
| Тръстеник — дължина на критичния път | 1 ден | **255 дни** |
| Тръстеник — критични задачи (от 895 листа) | 1 | **31** |
| Тръстеник — задачи, които свършват на последния ден с резерв > 0 | 98+ | **0** |
| Илиянци — дължина на критичния път | 1 ден | **780 дни** |

Срокът и флаговете НЕ се менят от поправката: Тръстеник си остава 255 дни
(45 + 210), Илиянци — 780 (120 + 660), и двата чисти по 12/12 флага. Тоест
поправено е описанието, не резултатът — точно както трябва да бъде.

Кодът: `src/ai_processor.py`, `compute_critical_path` е преместена след
`sync_durations_to_span`. Когато сметката не успее, това вече се КАЗВА, вместо
да остави мълчаливо старите стойности.

---

## 7а. Дефект №2: латинско „K" в кирилски етикет — ПОПРАВЕН

Намерен на 31.08 случайно, докато сравнявахме модели. Проверява се с една
команда и има пряко следствие върху количествата.

В `Ситуация Вод.pdf` етикетите на клоновете изглеждат еднакво, но не са:

```
Кл.  (кирилско К)   20 срещания
Kл   (ЛАТИНСКО K)   10 срещания
```

Тоест **една трета от водопроводните клонове са изписани със смесена азбука**.
Нашият четец ги търси с `^Кл\.` (кирилица) и мълчаливо ги пропуска —
`src/situation_reader.py:218`. Пропуснат клон значи липсващи метри, липсващ
участък и по-къс график, без нито едно предупреждение.

В целия тръжен пакет 68 думи от 177 598 са със смесена азбука, но те падат
точно върху означенията, по които се хващаме: `Kл` (10×), `OТ` (4×), `РP`
(18×), `TП` (4×), `Бaлчо` (4×). Това е обичаен артефакт от набора на чертежа,
не небрежност на възложителя.

### Поправката и числата

`src/text_norm.py` кирилизира омоглифите на единственото място, през което
текстът на чертежа влиза в четците (`_lines_with_boxes`). Две предпазни
правила, защото сляпата замяна е по-опасна от дефекта:

1. Заменя се САМО вътре в дума, която вече съдържа кирилица. `PEHD`, `DN160`,
   `HDPE`, `CCTV`, `L=150м`, `Q=10.00 l/s` остават недокоснати — кирилизация
   на `PEHD` би счупила всяко съпоставяне по материал.
2. Самостоятелна единична буква се поправя само на ред, който вече е
   български. Това е заради наставките: чертежът пише веднъж `Кл.8 a` с
   латинско `a` и веднъж `Кл.8а` с кирилско, и без правилото един клон става
   два.

Мерено на `Ситуация Вод.pdf`:

| | преди | след |
|---|---|---|
| прочетени етикети на клон | 108 | **113** |
| различни клонове | 46 | **49** |

Възстановените са истински и носят количества: `Кл.7а` (L=150 м), `Кл.20а`
(L=225 м), `Кл.8а`. Дотогава тези метри просто не съществуваха за системата.

Поправката не е тиха: всяко сменено означение влиза в лога, защото мълчаливата
поправка на чужд документ е същият клас проблем като мълчаливото изпускане,
само от другата страна.

Остава да се приложи същата нормализация и в описа на произхода и в
съпоставянето с КСС — там още сравняваме низове буквално.

Как беше намерен, защото методът е по-полезен от находката: сравнявахме шест
модела по това дали върнатите имена на улици ги ИМА буквално в документите.
Четири от шестте „измислиха" `ул. Балчо Нейков`. Оказа се, че в документа пише
`ул. Бaлчо Нейков` с латинско `a` — моделите бяха прави, а проверката ни
грешеше. Същият клас грешка чака навсякъде, където сравняваме низове.

---

## 8. Цената: генерирането е безплатно, четенето струва стотинка

Измерено на 26.08 с истинския рутер (не с подставения от сухия прогон):

| стъпка | повиквания | цена |
|---|---|---|
| конвертиране на документите | 0 | $0 |
| класификация на файловете | 0 (по ключови думи) | $0 |
| четене на ситуационните чертежи | 0 (векторно) | $0 |
| **анализ на тръжните документи** | 1 | **$0.0082** |
| **генериране на графика** | **0** | **$0.0000** |
| целият останал път | 0 | $0 |

Генерирането за Тръстеник: **0 повиквания към модел, 1030 задачи, 16 секунди.**
Причината е в кода и е нарочна — когато клоновете са известни (`партицията е
дадена`), `generate_packages` минава детерминистично и не пита модел. Плаща се
само когато участъците трябва да бъдат ИЗМИСЛЕНИ: тогава медианата от 40-те
записани прогона е ~$0.016.

Анализът на 790 000 знака документи става 23 463 входни токена след контекстния
бюджет и трае 64 секунди.

**Следствие за одита:** въпросът „кой модел ползвате" вече има много по-малко
тежест, отколкото при предишния брийф. Един-единствен прочит на документите е
всичко, което моделът прави в чист прогон.

---

## 8а. Кой модел чете документите — и защо изборът вече тежи малко

Тъй като анализът е единственото платено място, сравнихме шест модела върху
него (`tools/bench_analysis.py`, Илиянци, 1.6 млн знака). Оценката е срещу
неща, които в документите или ги има, или ги няма: типът на поръчката,
обявените срокове, обхватът, чист ли е JSON-ът и колко от върнатите имена на
места се срещат буквално в текста.

| модел | точки | чист JSON | места | непотвърдени | цена | сек |
|---|---|---|---|---|---|---|
| openai/gpt-5.6-luna | 10/10 | да | 36 | 2 | $0.0096 | 41 |
| x-ai/grok-4.3 | 10/10 | да | 23 | 2 | $0.0318 | 14 |
| deepseek/deepseek-v4-flash (сегашният) | 9/10 | — | 27 | 1 | $0.0024 | 51 |
| google/gemini-3.6-flash | 9/10 | — | 24 | 2 | $0.0375 | 23 |
| anthropic/claude-sonnet-5 | 9/10 | — | 26 | 0 | $0.1188 | 83 |
| openai/gpt-5-mini | 8/10 | да | 32 | 5 | $0.0206 | 59 |

**И шестте хващат вярно типа (инженеринг), обявените срокове (120 и 660) и
обхвата.** Тоест смяната на модела не мени графика — критичните факти не са
ограничени от модела. Разликата е в две неща: минава ли JSON-ът без спасителния
парсер и колко имена на места намира.

По един прогон на модел, тоест шумът не е премерен — за правило искат три
прогона на два обекта. Не го обявяваме за решение, а за наблюдение.

Тази таблица роди и находката от 7а: четири от шестте бяха обвинени, че
измислят улица, а грешеше нашата проверка.

---

## 9. Едно измерване, което противоречи на интуицията

Проверявахме правило „брой екипи = метри ÷ (под на темпото × дни)". Излезе
вярно за водопровода и грешно за канала, и грешката е поучителна:

| канализационни екипи | темпо | работа във веригата | срок на обекта |
|---|---|---|---|
| **3** | 4184 ÷ 210 ÷ 3 = **6.64 м/ден** | **635 екипо-дни** | **253 дни** |
| **5** | 4184 ÷ 210 ÷ 5 = 3.98 → подът **4** | **1060 екипо-дни** | 361 дни |

**Повече екипи правят повече работа.** Правилото „темпо = метри ÷ дни ÷ екипи"
го носи в себе си: всеки нов екип забавя темпото на всички, а сборът остава
`дни × екипи`. Докато темпото е над пода, това се компенсира; стъпи ли на пода,
забавянето спира, но работата вече е раздута.

Затова сметката с метрите дава ТАВАНА на екипите, не нужния брой. Правилото
не е прието — стои зад `CREWS_FROM_METRES=1`, по подразбиране изключено, а
измерените числа са записани в docstring-а, за да не се пробва наново.

Това е и въпрос към Вас (11.3): при положение че темпото се дели на екипите,
броят екипи не бива ли да идва САМО от изпълнителя?

---

## 10. По-дребните неща, поправени в периода

**Раздаване на клоновете.** Екипите вече работят както в човешките графици:
свободният екип взима следващия ГОТОВ клон, вместо да чака своя. Проверено
срещу еталонен `.mpp`: 12 екипа, всички започват в един и същи ден, нула
празнини между клоновете, нула застъпвания, по 7–12 клона всеки. Нашият изход
за Тръстеник: 13 екипа, 109 клона, 1816 екипо-дни в 253 дни, без празнини.

**Дефекти в износа към MS Project** — намерени, като отворихме собствения си
файл в MS Project, а не в текстов редактор:

| симптом в MS Project | причина | поправка |
|---|---|---|
| продължителности в ЧАСОВЕ | `DurationFormat=5` е часове | `7` = дни |
| колоните „Начало (ден)"/„Край (ден)" празни | ID-тата на `Number` полета се смятаха със стъпката на текстовите (+3) | числовите вървят с +1 |
| графикът се разписва НАЗАД от края | липсваше `<ScheduleFromStart>1` | добавено |
| 328 задачи показват „0 дни" | тип 0 (fixed units) преизчислява от труд | `Type=1`, `EffortDriven=0` |

**Ръчен вход без файл.** За график вече не е нужен КСС — 14 числа стигат.
Тръстеник е произведен точно така.

**Ред на мрежите.** Каналът тръгва от заустването нагоре, едрите тръби първи;
водопроводът влиза там, където каналът вече е излязъл, вместо да чака целия.

---

## 11. Какво НЕ можем да покажем и какво питаме

### 11.1 Записаната телеметрия е от стара версия

Два теста стоят червени НАРОЧНО и това е признанието им:

```
40 записани прогона са без manifest_id — не може да се каже от коя версия са.
32 оценени прогона нямат template_applicability_ok — техният „clean" не е по
днешното определение.
```

Тоест числото „15 чисти от 40" описва архитектура, която вече не съществува.
Нова серия иска живи прогони; при днешната цена това е под $1, но часове
време. Не сме я пуснали.

Освен това **записаната цена занижава**: провалените прогони се пишат с
`cost: 0.0`, макар да са изгорели токени. Записано $0.4595, по токени
$0.6333 — разлика 37 %.

### 11.2 Въпрос: кой източник на продължителности да е водещ

Днес обявеното темпо надделява над всичко, а 822 от 1123 продължителности идват
от медианите на веригите, не от верифицираните норми. Питаме:

* приемливо ли е за тръжен график продължителността да е следствие от срока, а
  нормата да служи само за проверка „възможно ли е изобщо"?
* ако не — как да се документира произходът на 821 задачи без доказателство,
  така че описът да остане честен?

### 11.3 Въпрос: броят екипи вход ли е или резултат

Раздел 9 показва, че при нашето правило повече екипи удължават графика. Нашето
предложение: броят екипи да идва само от изпълнителя, а системата да казва
единствено „при N екипа темпото става X м/ден — реалистично ли е". Съгласни ли
сте?

### 11.4 Остават отворени от предишния брийф

* **Ресурсните бройки** още са `max(предположение, наблюдаван връх)`, не
  потвърден парк. Трите отделни стойности, които предложихте
  (`observed_peak_reference`, `assumed_capacity`,
  `contractor_confirmed_capacity`), не са влезли.
* **Level-of-effort задачите** са направени като един непрекъснат ред, но без
  quantity-driven деца, както предложихте.
* **29 % от количествата нямат норма** (безизкопно, диаметри извън трите).
* **Библиотеката с технологични описания** (1491 операции от изпълнените
  работни програми, ≈1494 страници) НЕ е изчистена от следи на чужди обекти.
  Докато не е, тя не бива да влиза в подавано предложение — рискът е да се
  подаде текст, който назовава друг град.

---

## 12. Състояние на кода

```
2794 теста в 121 файла · 2790 минават · 2 пропуснати · 2 червени (11.1)
pre-commit hook пуска всички unit тестове при всеки комит
82 комита в периода
```

Двата червени теста са НАРОЧНО червени и са описани в 11.1: те не твърдят, че
кодът е счупен, а че записаните прогони са от стара версия. Ще станат зелени
чак когато пуснем нова серия.

Инструментите, с които всяко твърдение тук може да бъде проверено:

| инструмент | какво мери | цена |
|---|---|---|
| `tools/offline_dry_run.py` | целият път след модела, срок и флагове | $0 |
| `tools/read_mpp.py` | чете човешки `.mpp` през MS Project | $0 |
| `tools/compare_calendars.py` | сравнява календари, за да не се повтори грешката „558 срещу 780" | $0 |
| `tools/bench_analysis.py` | кой модел чете документите вярно | ~$0.20 |
| `tools/rerun_series.py` | четирите серии живи прогони | ~$0.50 |

---

# Раздел 14 — по Вашето становище от 31.08.2026

Приемаме го изцяло. Четири от бележките вече са вложени в кода и премерени;
две са планирани и казваме кога. Нищо не оставяме само в текста.

## 14.1 Feasibility duration ≠ bid duration — ВЛОЖЕНО

Вашите пет полета съществуват на всяка задача:

```
base_duration           колко трае по доказателства
base_duration_source    какво е доказателството
bid_duration            колко трае в подадения график
calibration_factor      bid ÷ base
calibration_reason      защо са различни
```

`base_duration` се записва **преди** обявеното темпо да пипне каквото и да е
(`src/ai_processor.py`, веднага след `scale_segment_overhead`), и повторно
щамповане не я презаписва. Оттам нататък изречението, което системата може да
каже за всяка задача, е точно Вашето:

> „По наличните доказателства тази работа е 13 дни. За да се вмести офертата,
> графикът я планира на 10.5 дни, защото темпото е изведено от договорния срок
> и заявените екипи."

## 14.2 Стълбицата на основанията — ВЛОЖЕНА, и числото се смени драстично

Приехме Вашата стълбица дословно (`src/duration_evidence.py`):

```
PARAMETRIC_NORM · HISTORICAL_CORPUS_MEDIAN · CONTRACTOR_INPUT
CONTRACT_SPAN_CALIBRATED · DEFAULT_ASSUMPTION · UNSUPPORTED
```

Същият прогон на Тръстеник, само нов разрез:

| | преди | сега |
|---|---|---|
| „задачи без доказателство" | **821 от 895** | — |
| норма по DN/материал/метод | 67 | **74** |
| медиана от корпуса | не се броеше | **788** |
| наистина без основание | — | **33** (188 дни) |
| дял на доказателство | 7 % | **96 %** |

Разликата не е в кода, който смята — тя е в това, че `NOT_PARAMETRIC` се четеше
като „няма доказателство", а значи само „няма норма". Медианата от 22 изпълнени
графика е доказателство; просто не е норма. Точно както написахте.

Отделно и НЕ смесено с горното: **447 задачи са местени заради срока**,
Σ 3562 доказани задача-дни → 3368 в офертата (**×0.95**). Тоест офертата е
5 % по-стегната от доказателствената база — това вече е число, което може да
се защити пред възложител.

## 14.3 „Може ли доказуемо да го изпълним" — ВЛОЖЕНО

Срокът остава hard constraint, но вече не се произвежда мълчаливо. Всяко
изведено темпо получава присъда срещу измереното от корпуса
(p25 5.8 · медиана 8.4 · p75 9.8 · връх 14.4 м/ден, 77 участъка):

```
OVERSTAFFED · FEASIBLE · REQUIRES_UPLIFT · NOT_SUPPORTED_BY_DATA
```

Пример, Тръстеник при нарочно тежък срок (90 дни, 2 канализационни екипа):

> `sewer_section: NOT_SUPPORTED_BY_DATA` — 23.24 м/ден е НАД всичко наблюдавано
> (14.4 м/ден) и иска 177 % над медианата. Нищо в 22-та изпълнени графика не
> подкрепя такова темпо. **За да влезе в доказания диапазон трябва 3 екипа
> повече (общо 5), или 214 дни вместо 90, или друг метод/по-голямо застъпване.**

При обявените 210 дни двете вериги са `FEASIBLE` и нищо не се обявява.

Четвъртата присъда, `OVERSTAFFED`, е Ваша индиректно: тя хваща обратната
грешка, която измерихме на 26.08 — темпото се дели на екипите, затова всеки
излишен екип НАДУВА работата (канал: 3 екипа → 635 екипо-дни → 253 дни;
5 екипа → 1060 екипо-дни → 361 дни).

## 14.4 Екипите са вход; сценариите са подкрепа за решение — ВЛОЖЕНО

`deadline_pace.scenarios()` слага числата на масата, без да решава:

```
водопровод 11 662 м, 210 дни        канализация 4 184 м, 210 дни
 3 екипа  18.51 м/ден  НЕ            1 екип   19.92 м/ден  НЕ
 4 екипа  13.88 м/ден  на ръба       2 екипа   9.96 м/ден  на ръба
 6 екипа   9.26 м/ден  ДА            3 екипа   6.64 м/ден  ДА
10 екипа   5.55 м/ден  ДА            5 екипа   3.98 м/ден  в повече
```

Изпълнителят обяви 10 + 3. Минимумът по доказателства е 6 + 3 — тоест
декларацията му е консервативна по водопровода и точна по канала.

Това поправи и едно наше противоречие: досега при задействан под системата
казваше „10 екипа са в повече… стигат 10", защото смяташе броя, при който
темпото стъпва точно на пода — най-бавното допустимо. Сега мерилото е корпусът
и отговорът е 6.

**Остава:** сценарийната таблица да се показва в самия въпрос към човека, а не
само в прогона. Това е UI работа в `chat_handler` и е следващото, което ще
направим.

## 14.5 22-project benchmark вместо 40 повторения — ПРИЕТО, НЕ Е НАПРАВЕНО

Съгласни сме, че това е най-важното практическо следствие. Сега е възможно и
евтино: при известни участъци генерирането е 0 повиквания и $0, тоест 22
проекта струват колкото едно кафе и се въртят колкото трае процесорът.

Какво липсва, за да го пуснем: за 22-та стари проекта имаме ИЗПЪЛНЕНИТЕ
графици (четем ги през MS Project), но не и тръжните им входове в машинно
четим вид за всичките. Първата стъпка е инвентаризация кои от 23-те обекта имат
и вход, и изход. Ще Ви дадем списъка, преди да строим суита — за да се
разберем по мерките, преди да сме ги оптимизирали.

Мерките, които предлагате, приемаме както са изброени, с една добавка:
**дял на калибрираните продължителности и среден `calibration_factor`** — за да
се вижда колко от съвпадението идва от доказателства и колко от напасване.

## 14.6 Формулировката за веригите — ОТТЕГЛЕНА

Раздел 5 вече казва „корпусът подкрепя продължителностите на основните ни
технологични вериги" и изрично изброява какво още би трябвало да сравним, за да
имаме право на думата „верни".

## 14.7 Какво според нас е следващият истински въпрос

Съгласни сме с формулировката Ви: не „може ли да направи график", а „може ли
за всяка съществена задача да обясни защо е там, защо трае толкова и какви
ресурси иска — преди да я напасне към срока".

От трите части „защо трае толкова" вече има отговор (14.1–14.3). „Какви ресурси
иска" има частичен — бройките са наблюдаван връх, не потвърден парк (11.4).
„Защо е там" е най-слабото: пространственото основание на пакета се води по
`spatial_source`, но не се обяснява по същия начин като продължителността. Там
отиваме след сценарийната таблица.
