# Промени 18-19 Февруари 2026

Обобщение на всички промени по ВиК График Генератор за последните два дни.

## Ден 1: 18 Февруари 2026 — Изграждане на приложението от нулата

### Commit 1: Initial commit (8d593c6)
**Streamlit app с чат UI, конвертиране на файлове, knowledge система**
- Начална структура: 35 файла
- Streamlit интерфейс: чат (45%) + Gantt (55%) split layout
- Knowledge Manager: 3-нивова база знания (lessons → methodologies → skills)
- Демо данни и базов Gantt chart

### Commit 2: Self-evolution система (a3e1d84)
- AI може да модифицира собствения си код
- 3 нива защита: зелено (знания) / жълто (конфиг) / червено (код + админ код)
- Git backup преди промяна, автоматичен rollback при грешка
- Лог на промените в `evolution_log.json`

### Commit 3: Интерактивен Gantt chart (eb230da)
- 9 слоя визуализация с toggle checkboxes
- Критичен път (червено), зависимости (стрелки), milestones (диаманти)
- Филтри: по екип, фаза, тип + zoom (дни/седмици/месеци)
- Click-to-select с детайлен панел
- Таблица с условно форматиране, статистика, експорт таб

### Commit 4: Knowledge pipeline fix (fe33372)
- DeepSeek ВИНАГИ получава knowledge context (3 нива: minimal/full/verification)
- Стриктен JSON pipeline: конвертиране е задължителна първа стъпка
- Кеширане на knowledge файлове с timestamp проверка
- Скорошни 5 проекта с възобновяване от последен прогрес

### Commit 5: PDF и XML експорт (b080a0c)
- PDF експорт (A3 landscape): таблица + Gantt + критичен път + легенда
- MSPDI XML експорт: DurationFormat=5, Manual=1, Custom Fields, 7-дневен календар
- JSON експорт
- DejaVu Sans шрифт за кирилица в PDF
- Тестове за PDF и XML (`tests/test_exports.py`)

### Commit 6: Windows инсталатор (f90579b)
- `install.bat`, `start.bat`, `update.bat`
- Пряк път "ВиК Графици" на десктопа
- `.env.company` за фирмени API ключове
- `.streamlit/config.toml` с тема и настройки

### Commit 7: README, CHANGELOG, ARCHITECTURE (2781d67)
- Пълна потребителска документация на български
- Техническа архитектура с ASCII диаграми
- Auto-update система за документация

### Commit 8: Локална валидация (c1651fe)
- Пълна offline валидация на генерирани графици
- Errors + Warnings + Cascade анализ + Modification diff
- Не зависи от AI — работи и без интернет

### Commit 9: PyMuPDF за PDF конвертиране (cde756f)
- Замяна на PyPDF2 с PyMuPDF/fitz — значително по-добро извличане на текст
- 3-степенна стратегия: GOOD (fitz) → PARTIAL (DeepSeek reformat) → SCANNED (OCR)
- Тест с Герман (31 PDF): 27 GOOD, 1 PARTIAL, 3 SCANNED — 0 API извиквания за 90%

### Commit 10: Персистентна чат история (6939c8f)
- Последните 10 двойки съобщения се запазват за всеки проект
- Auto-save + auto-restore при page refresh
- Кумулативно проследяване на AI разходи (across sessions)
- Бутон "Спри операцията" за дълги AI процеси

## Ден 2: 19 Февруари 2026 — Стабилизация + E2E тестове

### Commit 11: Progress bar + smart loading (c1798cd)
- Progress bar за AI операции
- Зареждане на проект по име (не само по път)

### Незаписани промени (текущи, готови за commit):

#### Bug Fixes
- **Дублирана чат история**: Поправени 3 бъга, които причиняваха натрупване на welcome съобщения
  - Премахнат double restore в auto-restore блока
  - `_save_chat_history()` пренаписана — запазва само user-initiated exchanges
  - `_load_project_by_path()` skip-ва status message при auto-restore
  - `welcome_shown = True` при auto-restore предотвратява нов welcome
- **Browse бутон**: Добавен `-STA` flag за PowerShell — решава проблем с Windows Forms dialog
- **Browse бутон**: Error handling (timeout, exception)
- **Горица placeholder**: Премахнати всички референции към "Горица" тестов проект
  - `app.py`: placeholder `D:\Проекти\Име на проект` вместо `D:\Projects\Горица`
  - `chat_handler.py`: generic помощен текст без конкретни имена

#### Нов UI елемент — Gantt Chart подобрения
- `gantt_chart.py`: По-широк ляв margin (420px вместо 250px) за дълги имена на задачи
- `gantt_chart.py`: По-голям шрифт (11px вместо 10px) за labels
- Демо графикът използва КСС-style naming:
  - `Кл.1 Водопровод PE-HD Ф225 ул. Ал. Стамболийски (О.Т.45–О.Т.62)` вместо `Ф225 Главен водопровод`
  - Реални улични имена + DN диаметри + О.Т. референции

#### UI Wireframe документация
- **Нов файл**: `docs/UI_WIREFRAME.md` — ASCII wireframe на целия интерфейс
- Пълен регистър на 40+ интерактивни UI елемента
- Таблица: тип, етикет, key, default стойност, условие за видимост, ред в app.py

#### Playwright E2E тестове — 23 теста, 100% pass rate
- **Нов файл**: `tests/e2e/conftest.py` — Streamlit server fixture
  - Автоматично стартира сървър на порт 8502
  - Управление на `.env` (създава dummy при нужда, запазва реален)
  - Чака AI health check спинъра да изчезне (до 60 сек)
  - Screenshot при failure за диагностика
- **Нов файл**: `tests/e2e/test_page_load.py` — 3 теста (app container, sidebar, config check)
- **Нов файл**: `tests/e2e/test_sidebar.py` — 8 теста (AI Status, Разходи, Проект, Знания, Еволюция, Документация, бутони)
- **Нов файл**: `tests/e2e/test_chat.py` — 3 теста (heading, textarea, first message)
- **Нов файл**: `tests/e2e/test_visualization.py` — 4 теста (8 checkboxes, 4 selectboxes, Gantt, tabs)
- **Нов файл**: `tests/e2e/test_export.py` — 3 теста (PDF, XML, JSON бутони)
- **Нов файл**: `tests/e2e/test_project_flow.py` — 2 теста (visualization heading, schedule content)

#### Pre-commit hook
- **Нов файл**: `hooks/pre-commit` — bash script за автоматично стартиране на тестове
- **Нов файл**: `install-hooks.bat` — инсталатор за hook
- Инсталиран в `.git/hooks/pre-commit`

#### Зависимости
- `requirements.txt`: добавени `pytest>=9.0.0`, `playwright>=1.40.0`, `pytest-playwright>=0.5.0`
- `pytest.ini`: нова конфигурация за тестове

## Статистика

| Метрика | Стойност |
|---------|----------|
| Общо commits (2 дни) | 11 + 1 предстоящ |
| Нови файлове | 14 |
| Модифицирани файлове | 5 |
| E2E тестове | 23 (100% pass, 2 последователни рунда) |
| Unit тестове | 2 (PDF + XML export) |
| Версия | 0.8.0 → 0.9.0 |
